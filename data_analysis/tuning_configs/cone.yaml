method: bayes

early_terminate:
  type: hyperband
  min_iter: 2

metric:
  name: cum. accuracy
  goal: maximize

parameters:
  dist_scalar:
    min: 0
    max: 0.1
    distribution: q_uniform
    q: 0.0001